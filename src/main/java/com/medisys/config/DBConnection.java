package com.medisys.config;

/**
 * Single place that opens JDBC connections to SQL Server (Singleton pattern).
 *
 * Module : Shared - agree with the team before changing
 * Owner  : Whole team
 *
 * TODO: load db.properties from the classpath and return java.sql.Connection objects.
 */
public class DBConnection {
}
