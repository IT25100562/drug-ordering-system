package com.medisys.util;

/**
 * Runs once when Tomcat starts the app: checks the database connection and creates the uploads folder.
 *
 * Module : Shared - agree with the team before changing
 * Owner  : Whole team
 *
 * TODO: implement jakarta.servlet.ServletContextListener and add @WebListener.
 */
public class AppInitListener {
}
