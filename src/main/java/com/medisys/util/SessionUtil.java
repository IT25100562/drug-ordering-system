package com.medisys.util;

/**
 * Reads and writes the logged in user and one-time flash messages in the HTTP session.
 *
 * Module : Shared - agree with the team before changing
 * Owner  : Whole team
 *
 * TODO: currentUser(), login(), logout(), flash(), takeFlash().
 */
public class SessionUtil {
}
