package com.medisys.util;

/**
 * Small text helpers shared by all modules (HTML escaping, blank checks, number parsing).
 *
 * Module : Shared - agree with the team before changing
 * Owner  : Whole team
 *
 * TODO: html() escaping so user input is never printed raw in a JSP.
 */
public class TextUtil {
}
