<?xml version="1.0" encoding="UTF-8"?>
<!--
    MediSys - Online Medicine Ordering System

    Servlets and filters are mapped with @WebServlet / @WebFilter annotations,
    so this file only holds app-wide settings.
    Shared file - agree with the team before changing it.
-->
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>MediSys</display-name>

    <!-- Read form data and write pages as UTF-8 -->
    <request-character-encoding>UTF-8</request-character-encoding>
    <response-character-encoding>UTF-8</response-character-encoding>

    <!--
        Folder where uploaded prescription files are kept. It is outside the web
        folder on purpose, so files can only be opened through a servlet that
        checks who is asking.
        Leave empty to use the default:  <your home folder>/medisys-uploads
    -->
    <context-param>
        <param-name>uploadDir</param-name>
        <param-value></param-value>
    </context-param>

    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <session-config>
        <session-timeout>30</session-timeout>
        <cookie-config>
            <http-only>true</http-only>
        </cookie-config>
    </session-config>

    <error-page>
        <error-code>404</error-code>
        <location>/WEB-INF/views/common/error.jsp</location>
    </error-page>
    <error-page>
        <error-code>500</error-code>
        <location>/WEB-INF/views/common/error.jsp</location>
    </error-page>

</web-app>
